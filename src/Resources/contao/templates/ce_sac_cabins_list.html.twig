{% extends '@Contao/block_searchable' %}

{% block content %}

    <div class="card">
        {% if figure %}
            {{ figure|raw }}
        {% endif %}

        <div class="card-body">
            <p class="card-date m-0"><small></small></p>
            <a href="{{ jumpTo }}" title="weiterlesen">
                <h6 class="card-title mt-0">{{ cabin.name }}</h6>
            </a>
            <p><small>{{ cabin.altitude }} m.ü.M.</small></p>
        </div>
    </div>

{% endblock %}
